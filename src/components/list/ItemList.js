import React from 'react';

export default function ItemList({items}) {
    // return items.length ? (
    //     <ul>{items.map(i => <li key={i}>{i}</li>)}</ul>
    // ) : (
    //     <span>'no items'</span>
    // )

    return (
        // <ul>{items.map(i => <li key={i}>{i}</li>)}</ul>
        <ul>test</ul>
    ) 
}

